<h1>Listing describes</h1>
<p id="notice"><%= notice %></p>
<table>
  <tr>
    <th>Article</th>
    <th>Song</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @describes.each do |describe| %>
  <tr>
    <td><%= describe.article_id %></td>
    <td><%= describe.song_id %></td>
    <td><%= link_to 'Show', describe %></td>
    <td><%= link_to 'Edit', edit_describe_path(describe) %></td>
    <td><%= link_to 'Destroy', describe, :confirm => 'Are you sure?', :method => :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Describe', new_describe_path %>
